# Desktop 1.15.29 local delivery preparation

## Exact identities

- Reviewed runtime correction: `9d0be61151c55363915bcfc8f99ebc8ae0d53dfa` (CTO cleared its additive logout-barrier fix).
- Version-bumped source: `3b764e44deecf26f6bb5163c7fb61a49d05e59c2`.
- Source tree: `5b92fd1a613c126fb9c61d5939cd38317df5c263`.
- Branch: `codex/account-switch-1.15.29`; worktree: `apps/desktop/.worktrees/account-switch-completion`.
- The version commit changes only Desktop/OpenCode package versions and the two matching workspace versions in `bun.lock`. No dependency, runtime, workflow or permission changes.
- Read-only GitHub inspection found `desktop-beta-1.15.28` the newest published prerelease, no matching `desktop-beta-1.15.29` or `v1.15.29` refs, and no local 1.15.29 tag. This is a local candidate, not a remote version reservation.

## Local evidence

- Frozen-lockfile install with scripts disabled: passed without lockfile mutation.
- Full Desktop build on WSL Linux x64: passed, including native recovery CLI, shared node sidecar and Electron main/preload/renderer. Explicit `BHARATCODE_CHANNEL=beta`, `OPENCODE_CHANNEL=beta`, `OPENCODE_VERSION=1.15.29`, `BHARATCODE_SOURCE_SHA=3b764e44deecf26f6bb5163c7fb61a49d05e59c2`.
- `OPENCODE_RELEASE` and Sentry upload credentials were unset for the build. The legacy build-time model-data input was pinned to the existing checked-in `packages/opencode/test/tool/fixtures/models-api.json`, SHA-256 `189229a34d836d3bc87b120404617687e3804af5ee6e099b419104998f3fc273`, instead of fetching a mutable upstream snapshot. This is an explicit local diagnostic input, not a claim of official cohort parity or a runtime provider fallback.
- Electron Builder 26.8.1 / Electron 41.2.1 Linux x64 `--dir --publish never`: passed. Its recovery-CLI after-pack header/executable check passed. Expected absent optional other-platform packages and absent macOS-native resource-directory warnings were recorded; they are not macOS acceptance.
- Extracted app.asar manifest reports `@bharatcode/desktop` version `1.15.29`. Main/preload extracted bytes exactly match the local build outputs and contain the safe `account-status-changed` wiring. Packaged recovery bytes exactly match staged recovery bytes and have Linux ELF magic.
- Packaged recovery CLI `--version` in a newly created isolated test home: `1.15.29`.
- Fresh coordinator plus recovery-package contract tests: 14 passed / 0 failed, 59 assertions; Desktop typecheck passed. These are focused local checks, not another hosted CI run.
- No Electron application was launched. No installed profile, credentials, protocol handler, installed binaries, hosted workflow, release tag or publication was changed.

Local package directory: `packages/desktop/dist/linux-unpacked`.

| Artifact                             |     Bytes | SHA-256                                                            |
| ------------------------------------ | --------: | ------------------------------------------------------------------ |
| `resources/app.asar`                 | 129642611 | `fffcde5bde9d8427dfe39e9703b2e6e4361308b539c5627381ae2c8e10b2a87c` |
| `resources/bharatcode-opencode-cli`  | 144713856 | `02e23f20105806571dc8671d03dd66520063e479f473c68987ac671cfad6f55e` |
| `out/main/index.js` (inside asar)    |    222971 | `469fa95faf34e3a157308d6381bbd6c47b936310c9619bba545c87ae65745323` |
| `out/preload/index.js` (inside asar) |      6532 | `0a38dd4469a94947e5c508bc3ed07aff8fbcaa7f986b4a31e9ff8a6b038da9f8` |

## Minimal remaining delivery path

1. Recheck release/tag availability, then bind the existing cohort admission to the reviewed 1.15.29 source lineage and branch. Update its exact-version tests/digest and cohort version in a separate release-control change. Current workflow remains deliberately pinned to 1.15.28 and cannot admit this candidate; no dispatch was attempted. Do not silently relabel previous version-specific waiver evidence or call waived acceptance PASS.
2. Build same-source Windows x64 NSIS plus matching WSL runtime/helper and immutable manifests. Native Windows Node/Bun are present on this machine, but this turn did not prepare a separate native Windows checkout or produce a Windows installer. Do not install the Linux asar or reuse old-version helper binaries in the existing Windows installation.
3. Produce macOS arm64/x64 on the existing Developer ID signing/notarization path and Linux x64 AppImage/deb on the existing Linux producer. WSL cannot supply Apple signing/notarization evidence. No expensive hosted job was authorized or dispatched in this turn.
4. Before activation, perform approved normal installed first-attempt sign-out/sign-in to a different account, deliberately delay browser completion beyond the previous one-second poll, and verify Account and model views update without a second login. Include cancellation, restart/persistence, and normal installed protocol ownership checks. Use user-assisted authentication; do not borrow credentials or launch an isolated app that registers the user's protocol handler.
5. Only then complete immutable cohort/provenance review and separately authorized publication/website promotion. Current downloads and installed 1.15.28 remain unchanged.

The local code/build preparation is complete. Official multi-platform artifacts and installed real-account switch acceptance remain pending; there is no installed-login PASS or release-ready cohort claim.
