# Desktop 1.15.29 native local preparation

## Disposition

Local source admission, Windows NSIS packaging, and same-source WSL runtime preparation are complete. This is **not** installed account-switch acceptance, a hosted artifact attestation, a finalized multi-platform cohort, or publication approval.

This report supersedes only the pending admission/Windows-preparation portions of `2026-09-07-desktop-1.15.29-local-delivery.md`. Its earlier Linux unpacked diagnostic used a test model-data fixture and remains a separate diagnostic, not official producer parity. No artifact from that build was reused here.

## Frozen identities

- Reviewed account-session runtime: `9d0be61151c55363915bcfc8f99ebc8ae0d53dfa`.
- Version-only 1.15.29 source: `3b764e44deecf26f6bb5163c7fb61a49d05e59c2`.
- Prior diagnostic evidence: `25bbb62132b7a7beba025492c2c6a893c976663d`.
- Frozen candidate: **`239d0e91a73e6c924be35456e0906e23e30b871e`**; sole parent `25bbb62132b7a7beba025492c2c6a893c976663d`; tree `7e28b13c06a91ad40a5ee5b6ef80be67d040785d`.
- Candidate branch `codex/account-switch-1.15.29` remains at that source. This report belongs to the separate `codex/account-switch-1.15.29-evidence` branch and must not redefine the candidate source.
- Native checkout: `C:\Users\Shrey Gupta\source\repos\bharatcode-desktop-account-switch-1.15.29`, clean at the exact candidate/tree.
- WSL worktree: `/home/ubuntu/bharatcode/apps/desktop/.worktrees/account-switch-completion`.

The candidate commit changes exactly the cohort workflow and its test file. No application runtime, cohort validator, native producer steps, Apple credential/signing contract, or historical waiver receipts were changed.

## Admission and evidence boundaries

- Exact dispatch/head/source/sole-parent and two-file diff checks bind the candidate to the reviewed runtime ancestor, 1.15.29 Desktop/CLI versions, candidate branch, and `desktop-beta-1.15.29` tag.
- Both acceptance inputs are `required` only for this preparation. Old 1.15.28 waiver jobs and validators retain their historical meanings but are unreachable through the new admission. No old waiver was relabeled, no new waiver was invented, and no waived check is called PASS.
- Executable negative tests reject empty, historical 1.15.28, relabeled 1.15.29, `PASS`, and `OWNER_WAIVED` input values on either acceptance input.
- Cohort assembly/publication tests now use explicitly synthetic PASS-shaped contract fixtures rather than recycling old owner-waiver fixtures for a new version. These fixtures are test data, not evidence that Windows/WSL scenarios ran.
- The previous published rollback release is bound to `desktop-beta-1.15.28` at `55cce6826b0d44dd790c429fc57337a506e862b9`, verified through read-only GitHub release metadata.
- The upgrade harness's independently frozen current-beta fixture remains 1.15.25; its acceptance semantics were not broadened or relabeled in this two-file rebind.
- Draft-first, complete-cohort, immutable digest, signing, no-overwrite, and publication/website hold-by-default requirements remain unchanged. No hosted run or publication step was invoked.
- Workflow SHA-256: `668a6a596e09510e2532388bbee5bbcd17f117ca4f8ea5f69acae884ba279783`.

## Verification

- RED: both new admission tests failed against the previous workflow, including actual acceptance of the obsolete waiver input.
- GREEN/fresh exact candidate: `bun test test/distribution/lean-candidate-workflow.test.ts test/distribution/lean-cohort.test.ts` from `packages/opencode`: **40 passed, 0 failed, 636 assertions**.
- The exact admission shell was extracted and executed locally against the clean frozen source, with publication/website hold and both acceptance inputs required. It passed without any workflow dispatch.
- Native Windows Desktop typecheck passed. Native `src/main/account-session.test.ts`: **11 passed, 0 failed, 40 assertions**.
- Windows Desktop production build, host-native recovery CLI, shared Node sidecar, Electron main/preload/renderer, same-source WSL staging, and NSIS packaging with `--publish never`: passed.
- WSL candidate compiled with `--single --skip-install --skip-embed-web-ui --wsl-candidate`; its build smoke returned `1.15.29`.
- Installer Authenticode: **NotSigned**, no signer or timestamp certificate; ProductVersion `1.15.29`, consistent with the unsigned Windows policy.
- Read-only archive extraction of the NSIS payload proved embedded `app.asar`, recovery CLI, WSL manifest, and WSL binary exactly match the inspected unpacked outputs. The extractor reported the expected appended-data warning for the executable/archive container and exited 0 with all four requested payloads.
- Asar package version is 1.15.29; main/preload bytes exactly match build outputs and include safe `account-status-changed` wiring. Recovery helper is Windows PE and matches staged bytes. WSL runtime is ELF x64 with exact source/version/size/SHA-256 binding.
- NSIS-extracted recovery CLI `--version` returned `1.15.29` in a newly created synthetic home. No Electron app or installer was launched.
- `beta.yml` version, sole package, byte size, and SHA-512 match the installer; matching blockmap exists.
- Formatting, whitespace, source/tree identity, and clean native checkout checks passed.

## Reproducible local inputs and resolved host issues

- Windows Node `24.14.0`, Bun `1.3.14`; Visual Studio 2022 BuildTools with `Microsoft.VisualStudio.Component.VC.Tools.x86.x64` verified present. Frozen-lockfile hoisted native install completed, 1,491 packages, no lockfile change.
- Native clone initially had `core.symlinks=false`, so the tracked TypeScript symlink was a text placeholder and typechecking failed. Setting repository-local symlink handling and materializing the 14 tracked symlinks corrected the new checkout; Git remained clean. No source workaround was introduced.
- WSL system npm failed because its `semver` dependency is absent. An isolated official npm 10.9.2 tarball/runtime under `/tmp/bharatcode-11529-npm-runtime` supplied the executable fixture tests. No global npm links or installations were changed.
- Existing build-time model metadata was captured once from `https://models.dev/api.json` at `2026-09-07T07:43:01Z`: 4,495,465 bytes, SHA-256 `6d80ebb9099254192cb9689c4a8a9918b181c912f038901efe78bfc5afd92184`. Identical verified bytes supplied `MODELS_DEV_API_JSON` to native Windows and WSL builds. This is the existing producer's metadata input, not a shipped provider fallback or inference call.
- Standalone Git bundle SHA-256: `e53e09b6cf5f59675e783e84f9faf23537d1cdbf825f0c5afd8b78b8b54922a3`; verified complete history and exact source/tree on native clone.
- `OPENCODE_RELEASE`, hosted-action identity, GitHub tokens, signing credentials, and Sentry upload credentials were absent in the local build process; explicit beta/version/source/unsigned variables were supplied. No GitHub job identity was fabricated.
- Build warnings included existing Vite dynamic/static import and wasm sourcemap duplication, missing optional other-platform packages/macOS-native resource directory, and a native-module path-space warning; native rebuild and packaging subsequently completed successfully. These are not macOS or runtime acceptance claims.

## Local artifact checkpoint

Read-only copies of the installer, blockmap, and updater metadata are preserved in:

`C:\Users\Shrey Gupta\source\repos\bharatcode-desktop-1.15.29-local-inputs\artifacts`

Local input/inspection records and reviewable preparation scripts are adjacent under `bharatcode-desktop-1.15.29-local-inputs`. They explicitly identify themselves as local diagnostics, not hosted receipts.

| Artifact                                                   |     Bytes | SHA-256                                                            |
| ---------------------------------------------------------- | --------: | ------------------------------------------------------------------ |
| `bharatcode-desktop-win-x64.exe`                           | 221060011 | `e962e2d6aa418ecb4ca4aa2fee842f6110dc7aea5ab91e558bdddc44a8b5be60` |
| `bharatcode-desktop-win-x64.exe.blockmap`                  |    229636 | `30cfc691f77aa41d8e8344e67884080706382fedcc4ec0a085dc0464822c5ff4` |
| `beta.yml`                                                 |       359 | `03e24d106c6bb2f9d67362f15e404e9ec9a076c0373a28703a0a8ef8b184efc6` |
| `resources/app.asar`                                       | 262350686 | `d99cd73ffdf352afc9da571f64f65ae98dabbe9706d65e9b7a9cbfe64e4576ef` |
| `resources/bharatcode-opencode-cli.exe`                    | 145665024 | `a70c679e486130250f57089a1426e3cb33ee94a4c9bc96a8f3e2933f921defa2` |
| `resources/wsl-runtime/bharatcode-runtime-linux-x64-glibc` | 129419392 | `0f8f194183c179915a17c379acd5dc555c62f1002f16548719328fd96e5201ff` |
| `resources/wsl-runtime/manifest.json`                      |       244 | `9d7a5ea657f18c77b422fa083a80e4e5d8ebc290cf1e838909837b1f22c66984` |

## Remaining gates and non-actions

The installed 1.15.28 app.asar is unchanged: `cd1b75da346089d0f9b51b51cbb5dcad6412e14e69424ce91f942cf059b4cd1f`. No installed app/profile, protocol handler, credentials, live account session, release, website, or platform deployment was mutated.

The next functional test requires a separately approved normal installed update and user-assisted authentication: sign out, begin first sign-in/switch to the other account, delay browser completion beyond the former one-second behavior, and confirm the first callback updates Account/model views without another sign-in. Include cancellation, queued logout, restart persistence, and normal installed protocol ownership. Do not copy credentials or launch a diagnostic Electron process that registers the user's protocol.

macOS arm64/x64 still needs the existing Apple signing/notarization host. Linux AppImage/deb and a complete immutable hosted cohort are not produced by this local Windows preparation. Required acceptance jobs were not launched; any actual future blocker needs a concrete failure report and explicit owner decision, not a retroactive waiver or new framework. No install, hosted/multi-VM run, push, publication, or download activation is authorized by this checkpoint alone.
